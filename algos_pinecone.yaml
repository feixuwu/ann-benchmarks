float:
  euclidean:

    pinecone:
      docker-tag: ann-benchmarks-redisearch
      module: ann_benchmarks.algorithms.pinecone
      constructor: PineCone
      base-args: ["@metric"]
      run-groups:
        redisearch:
          arg-groups: [[1]]
          query-args: [[1,2,3,4,5]]